// Имена глифов. Отдельным файлом, потому что на него ссылается
// сгенерированный `icon_paths.dart`, а тот не должен зависеть от всей
// секции Foundation.

/// Глифы набора. Figma `Icon` (`68:14`), 19 вариантов.
///
/// Имена совпадают с вариантами в макете; `share-2` и `sliders-horizontal`
/// записаны в dart-регистре — дефис в идентификатор не годится.
enum DsIconName {
  check,
  warning,
  info,
  question,
  camera,
  eye,
  x,
  share2,
  slidersHorizontal,
  chevronRight,
  mapPin,
  image,
  arrowLeft,
  sign,
  chevronUp,
  expand,
  calendar,
  arrowUpDown,
  download,
}
